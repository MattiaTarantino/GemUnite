<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Project Interface</title>
<style>
    body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
    }

    .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        background-color: #fff;
        margin-top: 30px;
    }

    .header {
        text-align: center;
        margin-bottom: 20px;
    }

    .title {
        font-size: 36px;
        color: #333;
    }

    .content {
        padding: 20px;
        border: 1px solid #ccc;
        border-radius: 5px;
        background-color: #f9f9f9;
    }

    .footer {
        text-align: center;
        margin-top: 20px;
    }

    .back-link {
        color: #007bff;
        text-decoration: none;
        font-size: 16px;
    }

    .back-link:hover {
        text-decoration: underline;
    }
</style>
</head>
<body>
<div class="container">
  <header class="header">
    <h1 class="title">New Project</h1>
  </header>

  <div class="content">
     <%= render "form", project: @project %>
  </div>

  <div class="footer">
    <a href="<%= my_projects_user_projects_path(user_id: @user.id) %>" class="back-link">Back to Projects</a>
  </div>
</div>
</body>
</html>