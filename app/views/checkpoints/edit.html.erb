<h1>Editing checkpoint</h1>

<%= render "form", checkpoint: @checkpoint %>

<br>

<div>
  <%= link_to "Indietro", user_project_checkpoint_path(user_id: @user.id, project_id: @project.id, id: @checkpoint.id) %>
</div>
