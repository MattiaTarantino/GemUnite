<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
      body {
          font-family: Arial, sans-serif;
      }
      .table-container {
          width: 70%;
          margin: 20px auto;
      }
      .no-tasks {
          text-align: center;
          font-style: italic;
          color: #888;
          margin-top: 20px;
      }
  </style>
</head>
<body>
<%= render @checkpoint %>

<div class="container mt-4">
  <% if @checkpoint.tasks.present? %>
    <div class="table-container">
      <table class="table table-bordered">
        <thead class="table-light">
        <tr>
          <th>Nome</th>
          <th>Descrizione</th>
          <th>Stato</th>
        </tr>
        </thead>
        <tbody>
        <% @checkpoint.tasks.each do |task| %>
          <tr>
            <td><%= task.nome %></td>
            <td><%= task.descrizione %></td>
            <td><%if task.completato == true %>
                  <%= "completato" %>
                <% else %>
                  <%= "non completato" %>
                <% end %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p class="no-tasks">No tasks available.</p>
  <% end %>

  <div class="d-flex justify-content-between align-items-center mt-3">
    <% if @role == "leader" %>
      <%= link_to "Edit this checkpoint", edit_project_checkpoint_path(project_id: @project.id, id: @checkpoint.id), class: "btn btn-primary" %>
      <%= button_to "Destroy this checkpoint", project_checkpoint_path(project_id: @project.id, id: @checkpoint.id), method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure?" } %>
    <% end %>
    <%= link_to "New task",  new_project_checkpoint_task_path(project_id: @project.id, checkpoint_id: @checkpoint.id) %>
  </div>
</div>
<%= link_to "Torna al progetto", project_show_my_project_path(project_id: @project.id), class: "btn btn-primary" %>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

